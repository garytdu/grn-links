# grn-links — GRN common link redirector (grn_owner)
Stable links that survive changing cloudflared tunnel URLs. GitHub Pages → garytdu.github.io/grn-links/.
Edit `redirects.json` (slug → current_url) + push. No secrets, no project data, box IP never exposed.
